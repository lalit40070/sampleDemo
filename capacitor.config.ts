import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'sample-demo',
  webDir: 'dist/sample-demo',
  bundledWebRuntime: false
};

export default config;
